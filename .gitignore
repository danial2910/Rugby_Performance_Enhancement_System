# === Backend (Spring Boot / Maven) ===
backend/target/
# application.properties now contains NO real secrets (all values come from
# environment variables), so it is safe to commit and is needed for Render
# to find the property keys when it builds the jar.

# === Frontend (Node / Vue) ===
frontend/node_modules/
frontend/dist/

# === IDE ===
.idea/
*.iml
.vscode/
*.swp

# === OS ===
.DS_Store
Thumbs.db

# === Environment / secrets ===
.env
*.env.local
